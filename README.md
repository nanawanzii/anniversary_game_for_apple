# The introducation

This is a simple game produced by pygame to celebrate the first anniversary.



run the code:
```
# install the dependency
pip install pygame

# run the game
cd ~/path
python3 test.py
```
